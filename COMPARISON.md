# A / B 프롬프트 비교 실험 기록

같은 원본 `index.html`에 **막연한 요청(A)** 과 **구조화한 요청(B)** 을 각각 적용하고, 결과 파일을 같은 잣대로 측정했습니다.

## 실험 설계

| 항목 | 내용 |
|---|---|
| 원본 | `index.html` (23,128 bytes) — 수정하지 않고 보존 |
| A 작업 파일 | `before/index.html` (원본 복사본) |
| B 작업 파일 | `after/index.html` (원본 복사본) |
| 실행 조건 | 같은 모델, **서로 다른 대화**에서 독립 실행. 한쪽이 다른 쪽의 조건을 보지 못함 |
| 측정 방법 | ① 원본 대비 diff ② `node --test tests/game.test.cjs` ③ 브라우저에서 계산된 CSS 값·레이아웃 계측(375px 뷰포트) |

두 요청 모두 "화면 아래 조작 안내를 개선한다"는 **같은 목적**을 갖고 있습니다. 차이는 목적을 얼마나 검증 가능한 조건으로 적어줬는가뿐입니다.

---

## 요청한 프롬프트 전문

### A: 막연한 요청

```text
이 커피 배달 게임의 조작 안내를 더 보기 좋게 만들어줘.
```

### B: 구조화한 요청

```text
[현재 상황]
이 index.html은 커피를 받아 네 이웃에게 배달하는 게임이다.
HTML 파일 하나로 브라우저에서 실행된다.

[목표]
처음 플레이하는 사람이 화면 아래에서 조작 방법을 쉽게 읽게 하고 싶다.

[구체적인 변경]
1. 화면 아래 조작 안내를 다음 문구로 바꿔줘.
   "① 시작 누르기 → ② 방향키로 이동 → ③ 카페에서 E로 커피 받기 → ④ 이웃 앞에서 E로 배달하기"
2. 이 안내 문구의 글자 크기를 16px로 하고 진한 갈색으로 표시해줘.
3. 좁은 화면에서는 문구가 줄바꿈되게 해줘.

[유지할 것]
게임 속 마을, 캐릭터, 이동 속도, 배달 규칙, 버튼 기능은 유지해줘.
외부 라이브러리나 API를 추가하지 마.
파일을 나누지 말고 index.html 하나로 실행되게 해줘.

[완료 기준]
- 안내 문구가 요청한 순서와 글자 크기로 보인다.
- 화면이 좁아도 안내가 잘리지 않는다.
- 시작, 이동, 커피 받기, 배달, 일시정지, 초기화가 계속 작동한다.

[결과물]
index.html을 직접 수정해줘.
끝에 변경한 점과 직접 확인하지 못한 점을 짧게 알려줘.
```

---

## 측정 결과

| 측정 항목 | 원본 | A: 막연한 요청 | B: 구조화한 요청 |
|---|---|---|---|
| 파일 크기 | 23,128 bytes | 26,421 bytes (**+14%**) | 23,317 bytes (**+0.8%**) |
| 변경 규모 | — | **+87줄 / −1줄** | **2곳** (CSS `p` 규칙 + `<p>` 문구) |
| 안내 문구 | "시작을 누른 뒤 방향키 / WASD로 이동 · E / Space로 배달" | 키캡 3줄로 재작성 (`↑←↓→ 또는 WASD 이동` 등) | "① 시작 누르기 → ② 방향키로 이동 → ③ 카페에서 E로 커피 받기 → ④ 이웃 앞에서 E로 배달하기" |
| 글자 크기 | 11px | 12.5px (좁은 화면 **11px**) | **16px** |
| 글자 색 | `#56624a` (연녹회색) | `#6a7560` (연녹회색, 거의 그대로) | **`#4a2c17` (진한 갈색)** |
| 375px 화면 | — | 안내 3줄 중 **1줄이 `display:none`으로 사라짐** | 2줄로 줄바꿈, 잘림·가로 스크롤 없음 |
| 375px 게임 화면 높이 | — | 597px (안내 영역이 **215px** 차지) | **652px** (안내 영역 149px) |
| 버튼 개수·id | 8개 / `start`·`pause`·`reset`·`action` | 8개, id 유지 (배치·순서·색·아이콘 변경) | 8개, **완전 동일** |
| 배달 규칙 테스트 | 3/3 통과 | **3/3 통과** | **3/3 통과** |

### A가 요청하지 않았는데 바꾼 것

- `<nav>` 요소를 통째로 `<div class="panel">`로 교체
- 방향 버튼을 십자(D-pad) 그리드로 재배치, DOM 순서 변경(`action` 버튼이 첫 번째로 이동)
- 버튼에 그라데이션·그림자·hover/active 효과, 아이콘(▶ ❚❚ ↺ ☕) 추가
- `시작/계속`을 초록 강조색, `커피 받기/전하기`를 커피색으로 변경
- `aria-label` 문구 변경("위" → "위로 이동")
- `@media(max-width:620px)` 규칙 추가 — **좁은 화면에서 안내 한 줄을 숨김**
- `main`의 `justify-content:center` 제거

### B가 바꾼 것 (전부)

```css
/* 변경 전 */ p{font-size:11px;margin:0 8px 8px;color:#56624a}
/* 변경 후 */ p{font-size:16px;line-height:1.6;margin:0 8px 10px;color:#4a2c17;
               flex-shrink:0;text-align:center;max-width:100%;white-space:normal;
               word-break:keep-all;overflow-wrap:break-word}
```

그리고 `<p>` 안의 문구 한 줄. 그 외 바이트 단위로 원본과 동일합니다.

---

## A와 B 비교하기

| 확인할 점 | A: 막연한 요청 | B: 구조화한 요청 |
|---|---|---|
| **내가 원한 문구·크기가 적용되었나요?** | ❌ 아니요. 문구를 AI가 임의로 재작성했고(①~④ 순서 없음), 크기는 12.5px·좁은 화면 11px로 요청한 16px에 못 미침. 색도 원본과 거의 같은 연녹회색이라 "잘 안 보이는" 문제가 그대로 남음 | ✅ 예. 문구·16px·`#4a2c17` 진한 갈색이 계산된 스타일로 정확히 확인됨 |
| **원하지 않은 곳도 바뀌었나요?** | ⚠️ 많이 바뀜. 87줄 추가, `<nav>` 구조 교체, 버튼 색·아이콘·순서·`aria-label`·레이아웃까지 변경. 375px에서 안내 영역이 215px를 차지해 **게임 화면이 55px 줄어듦** | ✅ 아니요. CSS `p` 규칙과 문구 한 줄, 딱 2곳 (+189 bytes) |
| **기존 조작이 유지되나요?** | ⭕ 예 (구조 변경에도 불구하고). 버튼 id와 `data-dir`을 보존해 스크립트가 그대로 동작하고 규칙 테스트 3/3 통과. 다만 조작을 유지한 것은 **요청이 아니라 AI의 판단**이었음 | ✅ 예. 게임 코드가 원본과 바이트 단위로 동일하므로 동작이 달라질 여지 자체가 없음. 테스트 3/3 통과 |
| **추가 설명을 몇 번 해야 했나요?** | **0회 만에 "완료"했지만, 목표에 도달하려면 최소 3회 더 필요** — ① 문구를 ①~④ 순서로 교체 ② 16px·진한 갈색 지정 ③ 좁은 화면에서 안내를 숨기지 말고 줄바꿈. (버튼 디자인을 원복하려면 4회) | **0회.** 한 번에 완료 기준 3개를 모두 충족 |

> 마지막 행의 A "3회"는 실제로 후속 요청을 돌려본 횟수가 아니라, **위 표에서 미달로 판정된 항목 수**를 근거로 센 값입니다.

---

## 읽어낸 것

1. **막연한 요청도 "그럴듯한 결과"는 낸다.** A의 결과물은 혼자 보면 오히려 더 예뻐 보입니다. 문제는 예쁜지가 아니라 *내가 원한 것이 되었는지*이고, 그건 요청에 판정 기준이 없으면 물어볼 수가 없습니다.
2. **모호한 요청의 비용은 "안 바뀐 것"이 아니라 "너무 많이 바뀐 것"으로 나타났다.** A는 아무것도 안 한 게 아니라 87줄을 바꿨고, 그중 요청과 관련된 부분은 일부였습니다. 되돌릴 것을 골라내는 일이 새 일감이 됩니다.
3. **가장 값을 한 조건은 [구체적인 변경]의 수치**였습니다. "보기 좋게" → 판정 불가. "16px, 진한 갈색" → 브라우저에서 바로 참/거짓을 잴 수 있음.
4. **[유지할 것]은 안전벨트 역할**을 했습니다. A도 우연히 조작을 유지했지만, 그건 보장이 아니라 운이었습니다. B는 애초에 게임 코드를 건드릴 이유가 없었습니다.
5. **다음에 추가하면 좋을 조건:** "좁은 화면에서 내용을 숨기지 말 것" — A가 `display:none`으로 한 줄을 지운 것은 "줄바꿈되게 해줘"라는 조건이 없었기 때문입니다. B의 [완료 기준]에 있던 "잘리지 않는다"가 정확히 이 사고를 막았습니다.

---

## 직접 확인하지 못한 것

- 프리뷰 창에서 캔버스가 실시간 갱신되지 않아 **실제 키보드 이동·E키 배달을 눈으로 확인하지 못했습니다.** 두 파일 모두 게임 스크립트가 원본과 동일하고 규칙 테스트가 통과하므로 동작에는 문제가 없을 것으로 보이지만, `before/index.html`과 `after/index.html`을 브라우저로 직접 열어 한 번씩 플레이해 확인하시기 바랍니다.
- A의 D-pad·강조 버튼이 실제 모바일 터치에서 어떤지는 측정하지 않았습니다.

## 재현 방법

```sh
node --test tests/game.test.cjs
```

```sh
npx --yes http-server . -p 5173
```
실행 후 `http://localhost:5173/before/`(A)와 `http://localhost:5173/after/`(B)를 비교합니다.
